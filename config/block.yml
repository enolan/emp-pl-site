# IP blocks for local devel/testing. Deployment is different!
defaultAllow: true
trustForwardedFor: false
# Can't only allow 127.0.0.1, because selenium comes from a container with its own IP
routeSpecs: []
